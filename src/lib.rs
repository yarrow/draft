#![cfg_attr(feature = "cargo-clippy", deny(clippy, clippy_pedantic))]
#![cfg_attr(feature = "cargo-clippy",
    allow(missing_docs_in_private_items, // This is the Markdown source
    ))]
//! See README
#![allow(dead_code)] // FIXME when we're done
mod code_filter;
